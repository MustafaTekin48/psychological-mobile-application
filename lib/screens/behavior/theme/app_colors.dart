import 'package:flutter/material.dart';

class AppColors {
  // Birinci renk (Canlı mavi)
  static const Color primaryColor = Color(0xFF4A90E2); // Canlı mavi

  // İkinci renk (Canlı şeftali)
  static const Color secondaryColor = Color(0xFFFF8B61); // Canlı şeftali

  // Aksesuar rengi (Canlı mint yeşili)
  static const Color accentColor = Color(0xFF00C9A7); // Canlı mint yeşili

  // Arka plan rengi (Beyaz)
  static const Color backgroundColor = Color(0xFFFFFFFF); // Beyaz

  // Ana yazı rengi (Canlı koyu gri)
  static const Color textColor = Color(0xFF4A4A4A); // Canlı koyu gri

  // Başlık rengi (Canlı mor)
  static const Color headingColor = Colors.white; // Canlı mor

  // Slider arka planı için yumuşak, hafif pastel turuncu ton
  static const Color sliderBackground = Color(0xFFFFCC80); // Pastel turuncu

  // Ek renkler (Canlı durum renkleri)
  static const Color successColor = Color(0xFF4CAF50); // Canlı yeşil
  static const Color warningColor = Color(0xFFFFC107); // Canlı sarı
  static const Color errorColor = Color(0xFFE74C3C); // Canlı kırmızı
}